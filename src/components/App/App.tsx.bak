import * as React from "react"
import { withRouter } from "react-router"
import { renderRoutes, RouteConfig } from "react-router-config"
import { RouteComponentProps } from "react-router-dom"

import { Timeline, Tween } from "react-gsap"
import { Controller, Scene } from "react-scrollmagic"

import Navigation from "@client/components/Navigation"
import PageDivider from "@client/components/PageDivider"
import CoachPage from "@client/pages/CoachPage"
import CoderPage from "@client/pages/CoderPage"
import CoderReferencesPage from "@client/pages/CoderReferencesPage"
import DoTogehterPage from "@client/pages/DoTogetherPage"
import EntrepreneurPage from "@client/pages/EntrepreneurPage"
import HomePage from "@client/pages/HomePage"
import LoveToDoPage from "@client/pages/LoveToDoPage"

import style from "./App.scss"

interface IAppProps extends RouteComponentProps {
  route: RouteConfig
}

interface IPageBlock {
  dividerTitle: string,
  dividerSubTitle: string,
}

const App: React.FunctionComponent<IAppProps> = ({ route }) => {
  return (
    <Controller>
      <Scene triggerHook="onLeave" duration="10000" pin={true}>
        <Timeline wrapper={<div className={style.app}/>}>
            <section className={style.contentContainer}>
                <div className={style.navContainer}>
                  <Navigation/>
                </div>
                 <div className={style.mainContentContainer}>
                  <HomePage/>
                </div>
            </section>
            <Tween from={{ y: "100vh", opacity: 0 }} to={{ y: "0%", opacity: 1 }}>
              <section className={style.contentContainer}>
                <PageDivider title="coder"/>
              </section>
            </Tween>
            <Tween from={{ y: "100vh"}} to={{ y: "0%"}}>
              <section className={style.contentContainer}>
                <div className={style.navContainer}>
                  <Navigation/>
                </div>
                <div className={style.mainContentContainer}>
                  <CoderPage/>
                </div>
              </section>
            </Tween>
            <Tween from={{ y: "100vh", opacity: 0 }} to={{ y: "0%", opacity: 1 }}>
              <section className={style.contentContainer}>
                <PageDivider title="coding"> references</PageDivider>
              </section>
            </Tween>
            <Tween from={{ y: "100vh"}} to={{ y: "0%"}}>
              <section className={style.contentContainer}>
                <div className={style.navContainer}>
                  <Navigation/>
                </div>
                <div className={style.mainContentContainer}>
                  <CoderReferencesPage/>
                </div>
              </section>
            </Tween>
            <Tween from={{ y: "100vh", opacity: 0 }} to={{ y: "0%", opacity: 1 }}>
              <section className={style.contentContainer}>
                <PageDivider title="agile"> coach</PageDivider>
              </section>
            </Tween>
            <Tween from={{ y: "100vh"}} to={{ y: "0%"}}>
              <section className={style.contentContainer}>
                <div className={style.navContainer}>
                  <Navigation/>
                </div>
                <div className={style.mainContentContainer}>
                  <CoachPage/>
                </div>
              </section>
            </Tween>
            <Tween from={{ y: "100vh", opacity: 0 }} to={{ y: "0%", opacity: 1 }}>
              <section className={style.contentContainer}>
                <PageDivider title="entrepreneur"/>
              </section>
            </Tween>
            <Tween from={{ y: "100vh"}} to={{ y: "0%"}}>
              <section className={style.contentContainer}>
                <div className={style.navContainer}>
                  <Navigation/>
                </div>
                <div className={style.mainContentContainer}>
                  <EntrepreneurPage/>
                </div>
              </section>
            </Tween>
            <Tween from={{ y: "100vh", opacity: 0 }} to={{ y: "0%", opacity: 1 }}>
              <section className={style.contentContainer}>
                <PageDivider title="what I am"> [...]</PageDivider>
              </section>
            </Tween>
            <Tween from={{ y: "100vh"}} to={{ y: "0%"}}>
              <section className={style.contentContainer}>
                <div className={style.navContainer}>
                  <Navigation/>
                </div>
                <div className={style.mainContentContainer}>
                  <LoveToDoPage/>
                </div>
              </section>
            </Tween>
            <Tween from={{ y: "100vh", opacity: 0 }} to={{ y: "0%", opacity: 1 }}>
              <section className={style.contentContainer}>
                <PageDivider title="what will we"> do together</PageDivider>
              </section>
            </Tween>
            <Tween from={{ y: "100vh"}} to={{ y: "0%"}}>
              <section className={style.contentContainer}>
                <div className={style.navContainer}>
                  <Navigation/>
                </div>
                <div className={style.mainContentContainer}>
                  <DoTogehterPage/>
                </div>
              </section>
            </Tween>
        </Timeline>
      </Scene>
      </Controller>
  )
}

export default withRouter(App)
